const { TwingExtensionMarkdown } = require('../../../../../../build/index');
const tap = require('tape');

tap.test('extension/markdown', function (test) {
    let extension = new TwingExtensionMarkdown();

    // TODO: not sure what I should be testing here...

    test.end();
});